/**
 * The browser UI lives in frontend/public/ (served by Express at /).
 * - index.html, styles.css, app.js
 *
 * This file used to be a React + shadcn stub with wrong API paths (/api/login vs /api/auth/login).
 * Open http://localhost:5000/ after starting: node server.js
 */
